rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Fotos de perfil: lectura pública, escritura solo por el dueño
    match /users/{userId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
