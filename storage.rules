rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Solo el usuario autenticado puede subir/actualizar su propia foto de perfil
    match /users/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
